{
  "name": "snapkit",
  "version": "1.0.1",
  "description": "A Figma plugin for managing prototype elements with absolute positioning, alignment, and component selection.",
  "private": true,
  "scripts": {
    "test": "node test/plugin.test.js && node test/ui.test.js"
  }
}
